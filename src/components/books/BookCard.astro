---
// ABOUTME: Book cover card component that links to the standalone book detail page.
// ABOUTME: Displays cover image, title, and author with hover effects.

import { Image } from 'astro:assets'

import type { BookData } from '@/utils/books'

import { readingPath } from '@/utils/paths'

interface Props {
  book: BookData
  grayscale?: boolean
}

const { book, grayscale = true } = Astro.props
---

<a class="group block w-full space-y-3" href={readingPath(book.id)}>
  <div
    class="relative aspect-2/3 w-full shrink-0 overflow-hidden rounded-lg bg-(--code-bg) shadow-md transition-all duration-500 group-hover:-translate-y-1 group-hover:shadow-xl"
  >
    <Image
      alt={book.title}
      class:list={[
        'h-full w-full object-cover transition-all duration-700',
        grayscale && 'grayscale group-hover:grayscale-0'
      ]}
      loading="lazy"
      src={book.cover}
      width={400}
    />
  </div>

  <div class="space-y-1">
    <h3 class="text-sm leading-tight font-bold transition-colors group-hover:text-muted">
      {book.title}
    </h3>
    <p class="text-[11px] font-medium text-faint">
      {book.author}
    </p>
  </div>
</a>
