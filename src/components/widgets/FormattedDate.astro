---
import type { FormattedDateProps } from '@/types'

import { formatContentDate } from '@/utils/date'

const { date } = Astro.props as FormattedDateProps
---

<time datetime={date.toISOString()}>{formatContentDate(date)}</time>
