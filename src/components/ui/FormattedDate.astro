---
import type { FormattedDateProps } from '@/types'

import { formatContentDate, toDateTimeAttr } from '@/utils/date'

const { date } = Astro.props as FormattedDateProps
---

<time datetime={toDateTimeAttr(date)}>{formatContentDate(date)}</time>
