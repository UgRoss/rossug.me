---
import { getEntry, render } from 'astro:content'

import { themeConfig } from '@/config'
import IndexLayout from '@/layouts/IndexLayout.astro'

const entry = await getEntry('pages', 'colophon')
if (!entry) {
  return Astro.redirect('/404')
}
const { Content } = await render(entry)
---

<IndexLayout
  description={entry.data.description}
  title={`${entry.data.title} - ${themeConfig.site.title}`}
>
  <div class="colophon-page-container mx-auto max-w-2xl py-8">
    <header class="mb-8">
      <h1 class="text-3xl font-bold text-(--text-primary)">{entry.data.title}</h1>
    </header>
    <article
      class="prose max-w-none text-(--text-primary) prose-neutral lg:prose-lg dark:prose-invert prose-headings:text-(--text-primary) prose-strong:text-(--text-primary)"
    >
      <Content />
    </article>
  </div>
</IndexLayout>
