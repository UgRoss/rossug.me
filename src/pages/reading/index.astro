---
import BookCard from '@/components/books/BookCard.astro'
import IndexLayout from '@/layouts/IndexLayout.astro'
import { getBooksForReadingPage, mapBookToData } from '@/utils/books'

const { currentlyReading, finishedBooks } = await getBooksForReadingPage()
const currentlyReadingCards = currentlyReading.map(mapBookToData)
const finishedBookCards = finishedBooks.map(mapBookToData)
---

<IndexLayout description="Reading list and book notes" title="Reading">
  <div class="space-y-12">
    <header class="space-y-2">
      <h1 class="text-xl">Books</h1>
      <p class="text-secondary">A collection of books I've read or am currently exploring.</p>
    </header>

    <div class="space-y-16">
      {/* Currently Reading Section */}
      {
        currentlyReadingCards.length > 0 && (
          <section class="space-y-6">
            <h2 class="text-xs font-semibold tracking-widest text-secondary uppercase">
              Currently Reading
            </h2>
            <div class="grid grid-cols-2 gap-x-6 gap-y-10 sm:grid-cols-3">
              {currentlyReadingCards.map((book) => (
                <BookCard book={book} grayscale={false} />
              ))}
            </div>
          </section>
        )
      }

      {/* Finished Books Grid */}
      {
        finishedBookCards.length > 0 && (
          <section class="space-y-6">
            <h2 class="text-xs font-semibold tracking-widest text-secondary uppercase">Finished</h2>
            <div class="grid grid-cols-2 gap-x-6 gap-y-10 sm:grid-cols-3">
              {finishedBookCards.map((book) => (
                <BookCard book={book} />
              ))}
            </div>
          </section>
        )
      }
    </div>
  </div>
</IndexLayout>
